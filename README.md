# EmberDB
EmberDB is a RAM-based, Redis-style database system written in C. It stores data as key-value pairs, allowing clients to connect over TCP and perform basic operations such as setting, retrieving, deleting, and saving data.
